<div class="button">
  <%= simple_form_for "", url: pethomes_search_path, :method => :get do |f| %>
   <%= f.text_field :search, value: params[:search], placeholder: "Where?", class: "btn-medium" %>
    <%= f.submit "Search", class: "btn btn-primary btn-treehouse" %>
  <% end %>
</div>
